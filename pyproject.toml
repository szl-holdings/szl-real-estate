[project]
name = "szl-re"
version = "0.1.0"
description = "SZL Real Estate — public-records underwriting. Not an MLS."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }

[tool.setuptools.packages.find]
include = ["szl_re*"]
